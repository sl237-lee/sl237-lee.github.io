############################################################
# Environment Configuration — Data Model & Pipeline Project
# Example configuration file for local or demo environments.
# ------------------------------------------------------------
# Copy this file as `.env` or `.local.env` and update values
# with your own environment credentials before running scripts.
############################################################

# -------------------------------------------------------------------
# Snowflake Configuration
# -------------------------------------------------------------------
SNOWFLAKE_ACCOUNT=demo_account.region_id.cloud_provider
SNOWFLAKE_USER=demo_user
SNOWFLAKE_PASSWORD=demo_password
SNOWFLAKE_ROLE=SYSADMIN
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=ANALYTICS
SNOWFLAKE_SCHEMA_STAGE=STG_TEMP
SNOWFLAKE_SCHEMA_SALES=STG_SALES
SNOWFLAKE_SCHEMA_DIM=STG_CUSTOMER

# -------------------------------------------------------------------
# AWS Configuration (for S3 landing zone / PrivateLink integration)
# -------------------------------------------------------------------
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# -------------------------------------------------------------------
# Optional: DBT / Prefect / Airflow Integration
# -------------------------------------------------------------------
DBT_PROFILES_DIR=./dbt
PREFECT_API_URL=http://127.0.0.1:4200/api
AIRFLOW_HOME=./airflow

# -------------------------------------------------------------------
# Notes
# -------------------------------------------------------------------
# 1. Never commit this file with real credentials.
# 2. Copy and rename it to `.env` or `.local.env` before execution.
# 3. Variables are automatically loaded by orchestration scripts.
# 4. Supports integration across Snowflake, AWS, and orchestration tools.